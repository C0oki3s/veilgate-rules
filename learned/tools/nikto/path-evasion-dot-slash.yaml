# ./ dot-slash traversal evasion — Nikto evasion mode 5/6.
# Tool: nikto | Feature: path_ngram
candidates:
  - id: VG-PATH-004
    feature: path_ngram
    bucket: ./.
    posterior: 0.9976190476190476
    support: 418
    active: true
    info:
      name: Dot-slash-dot traversal evasion variant
      author: community
      severity: medium
      description: |
        Alternative traversal encoding `./.` used to evade naive string
        filters that block `../`. Observed 418 times. Generated by tools with
        built-in filter evasion such as Nikto (`Evasions:5` and `Evasions:6`)
        and dotdotpwn's evasion modes. Semantically equivalent to `../` but
        bypasses many simple pattern-match rules.
      impact: |
        Evasion-aware path traversal. The client is specifically trying to
        bypass WAF or application-level `../` filters while achieving the same
        traversal effect.
      tags: [path_ngram, traversal, evasion, payload, attack, nikto]
      reference:
        - https://github.com/sullo/nikto/wiki/Nikto-Evasion-Techniques
      metadata:
        verified: "true"
        source: community
        false_positive_rate: low
        tool: nikto
