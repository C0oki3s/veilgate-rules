# ././. double dot-slash traversal evasion — deeper Nikto evasion variant.
# Tool: nikto | Feature: path_ngram
candidates:
  - id: VG-PATH-005
    feature: path_ngram
    bucket: ././.
    posterior: 0.9975490196078431
    support: 406
    active: true
    info:
      name: Double dot-slash traversal evasion variant
      author: community
      severity: medium
      description: |
        Deeper evasion variant `././.`. Observed 406 times alongside the `./.`
        variant (VG-PATH-004), suggesting systematic evasion enumeration by the
        same scanner session. This multi-segment encoding is generated by
        Nikto's built-in evasion modes and some fuzzing wordlists.
      impact: |
        Evasion-aware path traversal using multi-segment dot-slash sequences.
        Client is iterating through evasion variants to find a working bypass.
      tags: [path_ngram, traversal, evasion, payload, attack, nikto]
      reference:
        - https://github.com/sullo/nikto/wiki/Nikto-Evasion-Techniques
      metadata:
        verified: "true"
        source: community
        false_positive_rate: low
        tool: nikto
