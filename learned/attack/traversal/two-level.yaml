# Two-level ../../ path traversal — two directories above web root.
# Attack: path-traversal | Feature: path_ngram
candidates:
  - id: VG-PATH-002
    feature: path_ngram
    bucket: ../..
    posterior: 0.9993416721527321
    support: 1517
    active: true
    info:
      name: Two-level path traversal sequence
      author: community
      severity: high
      description: |
        The n-gram `../..` in a URL path, indicating a two-level directory
        traversal attempt. Observed 1,517 times. Generated by Nikto, Nuclei,
        dotdotpwn, and manual probing tools. Targets application files two
        levels above the web root — common for apps mounted in subdirectories
        (e.g. `/app/public/` → `../../etc/passwd`).
      impact: |
        Deeper traversal targeting application configuration files, database
        credentials, or OS files two directory levels above the web root.
      tags: [path_ngram, traversal, payload, attack, lfi, dotdotpwn]
      reference:
        - https://owasp.org/www-community/attacks/Path_Traversal
        - https://github.com/wireghoul/dotdotpwn
      metadata:
        verified: "true"
        source: community
        false_positive_rate: low
        tool: dotdotpwn
