# Single-level ../ path traversal segment — directory traversal / LFI.
# Attack: path-traversal | Feature: path_ngram
candidates:
  - id: VG-PATH-001
    feature: path_ngram
    bucket: ..
    posterior: 0.9994706193753309
    support: 1887
    active: true
    info:
      name: Single-level path traversal segment
      author: community
      severity: high
      description: |
        The segment `..` appearing in a URL path n-gram. Captured 1,887 times
        in the April 2026 dataset — the highest support of any traversal
        pattern. Automated tools inject `../` sequences to attempt directory
        traversal and read files outside the web root. Generated by Nikto,
        Nuclei, dotdotpwn, dirsearch, and manual testing.
      impact: |
        Path traversal attempt. Attacker may be trying to read server-side
        files such as /etc/passwd, application configs, private keys, or
        source code outside the document root.
      tags: [path_ngram, traversal, payload, attack, lfi, nikto, nuclei, dotdotpwn]
      reference:
        - https://owasp.org/www-community/attacks/Path_Traversal
        - https://github.com/wireghoul/dotdotpwn
      metadata:
        verified: "true"
        source: community
        false_positive_rate: low
